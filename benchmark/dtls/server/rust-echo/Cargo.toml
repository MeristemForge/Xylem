[package]
name = "bench-dtls-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "net", "io-util", "macros"] }
openssl = "0.10"
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
