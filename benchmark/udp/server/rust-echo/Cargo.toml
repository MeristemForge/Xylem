[package]
name = "bench-udp-rust"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["rt", "net", "macros"] }
clap = { version = "4", features = ["derive"] }

[profile.release]
opt-level = 3
lto = true
